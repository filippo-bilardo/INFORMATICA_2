#include "numeri.hpp"

int main() {
    Numeri numeri;
    numeri.aggiungiNumero(10);
    numeri.aggiungiNumero(20);
    numeri.aggiungiNumero(10);
    numeri.aggiungiNumero(20);
    numeri.aggiungiNumero(10);
    numeri.aggiungiNumero(20);
    numeri.aggiungiNumero(11);
    numeri.aggiungiNumero(99);
    numeri.stampaNumeri();

    return 0;
}